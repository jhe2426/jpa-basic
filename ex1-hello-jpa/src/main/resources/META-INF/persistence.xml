<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.2" xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">
    <persistence-unit name="hello">
        <properties>
            <!-- 필수 속성 -->
            <property name="jakarta.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:h2:tcp://localhost/~/test"/>
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>

            <!-- 옵션 -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.use_sql_comments"  value="true"/>

            <!--
                hibernate.hbm2ddl.auto 옵션
                - create: 기존 테이블 삭제 후 다시 생성 (DROP + CREATE)
                - create-drop: create와 같으나 종료 시점에 테이블 DROP
                - update: 엔티티 기준으로 DB 스키마에 없는 테이블/컬럼을 자동 추가하는 옵션(운영DB에는 사용하면 안 됨)
                    새 필드가 추가되면 alter table add column SQL이 실행될 수 있지만,
                    엔티티에서 필드를 삭제해도 기존 DB 컬럼을 자동으로 drop하지는 않는다.
                    컬럼명 변경이나 타입 변경 등을 안전하게 관리하기 어렵기 때문에 운영 DB에서는 사용하지 않는다.
                - validate: 엔티티와 테이블이 정상 매핑되었는지만 확인
                - none: 사용하지 않음

                운영 장비에는 절대 create, create-drop, update를 사용하면 안된다.
                개발 초기 단계는 create 또는 update
                테스트 서버, 스테이징과 운영 서버에는 validate 또는 none
                    스테이징: 운영 서버에 배포하기 전에, 운영 환경과 최대한 비슷하게 만들어서 마지막으로 테스트하는 환경
            -->
            <property name="hibernate.hbm2ddl.auto" value="create"/>
        </properties>
    </persistence-unit>

</persistence>
